# Notebooks

Use this folder for exploratory analyses that support **Workplace Learning Recommender**. Keep reusable logic in `src/` rather than hiding important transformations inside a notebook.

A useful notebook should state the question it answers, load data through a documented path, keep random seeds explicit, and end with a short interpretation that includes limitations.
